- QMB Requirements

   - QtNetSNMP libraries and headers.
   - Qt4 libraries and headers.
   - Makefile generation tool QMake.
   
   You can obtain Qt at http://www.qt.io/download/ or through  your package manager on unix systems. For QtNetSNMP download the lastest version from https://github.com/j2sg/qt-net-snmp

- Compilation

   - Generate a platform-specific makefile:

      qmake

   - Build the application:

      make
   
   Now we have the QMB binary ready to execute.

