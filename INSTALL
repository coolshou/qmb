- QMB Requirements

      - Net-SNMP libraries
      - Net-SNMP  headers
      - MIB files
      - Qt4 libraries
      - Qt4 headers
      - Makefile generation tool QMake.
        
        You can obtain Qt at http://qt.nokia.com/ or through 
        your package manager on unix systems.

- Compilation

   - Generate a platform-specific makefile:

      qmake

   - Build the application:

      - On Unix:

         make

      - On Windows:

         mingw32-make

   
   Now we have the QMB binary ready to execute.

